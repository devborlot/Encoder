# Credenciais do Peach (latam.peachvideo.com)
#
# Copie este arquivo para `peach_credentials.toml` e preencha com seus dados.
# O arquivo `peach_credentials.toml` é gitignored — NUNCA committe credenciais.
#
# Alternativa: defina via variáveis de ambiente:
#   PEACH_EMAIL, PEACH_PASSWORD, PEACH_PRODUCTORA_ID
# As env vars têm prioridade sobre o arquivo.

email = "seu-email@exemplo.com"
password = "sua-senha-aqui"

# ID da sua produtora no Peach (mesma para todos os clientes)
# Ex: "BRP190418" para Post.E Motion
productora_id = "BRP190418"
